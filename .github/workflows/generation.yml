name: Generate

on:
  - pull_request
  - workflow_dispatch

jobs:
  generate:
    name: Generate SDK
    runs-on: ubuntu-latest
    steps:
      - uses: speakeasy-api/sdk-generation-action@v0
        with:
          speakeasy-version: latest
          openapi-doc-location: https://docs.speakeasyapi.dev/openapi.yaml
          github-access-token: test
          languages: |-
            - go
